name: diagnostic-release

on:
  workflow_call:
    inputs:
      tag:
        required: true
        type: string
      expected_commit:
        required: true
        type: string
      receipt_name:
        required: true
        type: string

jobs:
  diagnostic:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "diagnostic reusable workflow compiled"
